apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sentinel-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/amrufai/sentinel
    targetRevision: main
    path: manifests/overlays/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: sentinel
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true